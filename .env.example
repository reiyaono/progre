# ovld_cp 環境変数テンプレート（実値は .env にコピーして記入）
# 詳細・キーの切り分け方針は docs/design/ops.md を参照。

# Supabase プロジェクトURL（クライアント露出可）
SUPABASE_URL=

# anon public key（クライアント露出可 / RLS が全テーブルで保護）
SUPABASE_KEY=

# service-role key（サーバ専用・絶対に公開しない / server/api のみで使用）
SUPABASE_SERVICE_ROLE_KEY=
